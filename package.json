{
  "name": "floin-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "python scripts/preprocess.py && python scripts/simulate.py --P 120 --CN 78 --t 45 && echo \"All module tests passed\"",
    "lint": "echo \"No JS lint configured - run build check\" && npm run build"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "geotiff": "^3.0.5",
    "leaflet": "^1.9.4",
    "three": "^0.185.1"
  }
}
