# 3D — FloodSimulation (~650 LOC + WebFlood 128²)

**Stack:** `Three 0.185 + OrbitControls damping 0.08 + geotiff 3.0 + RainParticleOverlay 600 drops` · `runtime nodejs` for DEM

**Terrain `Plane 14×14 72|90seg`:** `BASIN_PROFILE all/central/adyar/ennore/velachery/chembarambakkam {base 6.5→3.1→9.5, roughness, marsh 0→3.0, hill, urban}` + 4-octave `sin0.58/cos0.68` + AOI dome `exp(-d²/3.5)*1.85` + view warp `hydrology ridge sin4.2 / velocity sin2.5 / depth bowl / checker` → HSL per view `hydrology 14-band 0.58 / velocity 0.55 / depth 0.58→0.03 heat / ledger brown`. `computeVertexNormals` per AOI. `42 lands` `6×7` distinct.

**Water `Plane 13.4 64|40`:** `Shader time/depth/opacity/rippleCenter/Time` vert `sin1.1*t*0.035+ripple sin28*exp-6`, frag `shallow#0E7490→mid#E6B422→deep#E63946 + caustics pow(c1+c2,3)*0.35 + foam fract*0.12 + vRipple*0.6` GPU-only, scale `0.18-0.95` per AOI bounds.

**Buildings `90-520 cap/basin` (dynamic):** `Extrude bevel 0.01 depth levels*0.19 (+ennore 0.06)` per-basin mat `ennore #b8c0c8 metal0.18 / velachery #d6d3c4 / chem #c2b8a3` + `wardProb` dynamic (`wardForLngLat` → `wardDamage(ward, rainfall, cn)` `prob=Q/80`) drives `infrastructure_impact/depth_heatmap` (`>0.6 #991b1b, >0.3 #E6B422`), `frustumCulled+shadow`, `velocity_field` +18 `Cone #0E7490` arrows, `InstancedMesh` buckets per mat.

**Roads/Waterways:** shared `LineBasic depthWrite:false` `facc15/06b6d4` / `0284c7`, `frustumCulled`.

**Hotspots:** `Cylinder 0.08×0.6 emissive 0xd97706 pulsing 0.55+sin2.2*0.4` `terrainH+0.3` `O(n)` scan (bilinear next).

**Controls:** `OrbitControls 3-28 maxPolar 0.48π`, `requestAnimationFrame + Clock delta`, `hover emissive #E6B422 tooltip`, click `rippleCenter(uv)` + `onSelectObject`, double-click focus, `M` measure `LineDashed`, keys `Space/←→/R/F/M`, compass `getAzimuthalAngle°` + scale `dist*0.37/14*111/8 km`, `Rain 600 drops`.

**Cache:** `Map 20 LRU key id-xmin/xmax/ymin/ymax-P-CN-t-viewMode` + `requestId+AbortController` zero-base. Dynamic `wardDamage` now uses live `rainfall/cn`, not hardcoded `160/84`.

**FBO:** `Shallow-Water Engine 128² Float32 g9.81 dt0.02 n0.04` shallow-water `vel*0.98+slope*g*dt -manning*vel` in hydrology workspace.

**Perf:** `1024 shadow`, shared mats, `72/90 seg`, GPU water, next `BatchedMesh` + `Line2` + `WebGPU TSL`.
